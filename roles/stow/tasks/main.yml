---

- name: Install stow
  pacman:
    name: stow
    state: latest

- name: Symlink configs
  command: "stow -d {{ dotfiles }} -S {{ item }}"
  with_lines: "cat {{ dotfiles }}/stow.txt"
  become: no
